/* todo - Textual CSS スタイル

   色は端末のANSIパレット (ユーザーのターミナル配色設定) に追従させる。
   テーマ自体も ansi-dark を使っているため、ここで指定するのは
   パネルの区切りとレイアウトが中心。 */

Screen {
    background: ansi_default;
    layout: vertical;
}

#main {
    height: 1fr;
    margin: 0 1;
}

/* ── 左: TODOリスト ───────────── */
#task-panel {
    width: 1fr;
    min-width: 32;
    border: round ansi_bright_black;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

/* 操作対象のパネルが分かるように、フォーカス中は枠の色を変える */
#task-panel:focus-within,
#calendar-panel:focus-within {
    border: round ansi_blue;
}

#task-table {
    height: 1fr;
    background: ansi_black;
}

/* 一覧の下のメモ欄 (複数行。入りきらない分は m キーの全画面で読む) */
#memo-pane {
    dock: bottom;
    height: 5;
    padding: 0 1;
    border-top: solid ansi_bright_black;
    border-title-color: ansi_white;
    border-title-style: bold;
    border-subtitle-color: ansi_bright_black;
    overflow: hidden;
}

/* ── 右: カレンダー + 内訳 ────── */
#side-panel {
    width: 31;
}

#calendar-panel {
    height: 10;
    padding: 0 1;
    border: round ansi_bright_black;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

#summary-panel {
    height: 1fr;
    min-height: 6;
    padding: 0 1;
    border: round ansi_bright_black;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

/* ── 追加・編集モーダル ────────── */
TaskEditScreen {
    align: center middle;
    background: ansi_default;
}

#edit-dialog {
    width: 62;
    max-width: 90%;
    height: auto;
    /* 端末が低いときは項目が入りきらないのでスクロールさせる */
    max-height: 100%;
    overflow-y: auto;
    padding: 1 2;
    border: round ansi_blue;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

#edit-dialog Label {
    margin-top: 1;
    color: ansi_bright_black;
}

#edit-buttons {
    height: auto;
    margin-top: 1;
    align-horizontal: right;
}

#edit-buttons Button {
    margin-left: 2;
}

/* 入力欄とプルダウンは枠を省いて高さを詰めている (端末が低くてもフォームが
   収まるように)。代わりに背景で入力欄の範囲を示す */
#edit-dialog Input, #item-dialog Input,
#edit-dialog Select > SelectCurrent, #item-dialog Select > SelectCurrent {
    background: ansi_bright_black;
}

/* Select は枠線でしかフォーカスを示さない作りなので、枠を消すと変化が無くなる。
   開いたときの選択行と同じ配色 (block-cursor) で塗って分かるようにする。
   Input は Textual 側にフォーカス時の配色があるのでそれに任せる */
#edit-dialog Select:focus > SelectCurrent, #item-dialog Select:focus > SelectCurrent {
    background: $block-cursor-background;
    color: $block-cursor-foreground;
}

/* SelectCurrent の中身は自前の色を持っているので、フォーカス時はまとめて上書きする */
#edit-dialog Select:focus > SelectCurrent Static#label,
#item-dialog Select:focus > SelectCurrent Static#label,
#edit-dialog Select:focus > SelectCurrent .arrow,
#item-dialog Select:focus > SelectCurrent .arrow {
    color: $block-cursor-foreground;
}

/* 優先度とステータスは横に並べる */
#edit-selects {
    height: auto;
}

#edit-selects > Vertical {
    width: 1fr;
    height: auto;
}

#edit-selects > Vertical:last-of-type {
    margin-left: 2;
}

#tags {
    height: auto;
    max-height: 6;
    border: none;
    background: ansi_black;
}

#no-tags {
    color: ansi_bright_black;
}

/* フォームのメモ欄は控えだけ (本文の編集は全画面) */
#memo-row {
    height: 1;
}

#memo-preview {
    width: 1fr;
    height: 1;
}

/* ── メモの全画面表示・編集 ────────── */
MemoViewScreen, MemoEditScreen {
    background: ansi_default;
}

#memo-dialog {
    width: 100%;
    height: 100%;
    padding: 0 1;
    border: round ansi_blue;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
    border-subtitle-color: ansi_bright_black;
}

#memo-header {
    height: auto;
    padding: 0 1;
    background: ansi_bright_black;
}

#memo-body {
    height: 1fr;
    padding: 1;
    scrollbar-size-vertical: 1;
}

#memo-editor {
    height: 1fr;
    margin-top: 1;
    border: none;
}

/* ── 削除の確認 ─────────────────── */
ConfirmDeleteScreen {
    align: center middle;
    background: ansi_default;
}

#confirm-dialog {
    width: 52;
    max-width: 90%;
    height: auto;
    padding: 1 2;
    border: round ansi_red;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
    border-subtitle-color: ansi_bright_black;
}

#confirm-message {
    height: auto;
}

#confirm-buttons {
    height: auto;
    margin-top: 1;
    align-horizontal: right;
}

#confirm-buttons Button {
    margin-left: 2;
}

/* ── 設定画面 ───────────────────── */
SettingsScreen, ItemEditScreen {
    align: center middle;
    background: ansi_default;
}

#settings-dialog {
    width: 76;
    max-width: 96%;
    height: auto;
    max-height: 100%;
    overflow-y: auto;
    padding: 1 2;
    border: round ansi_blue;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

#settings-lists {
    height: 12;
}

#status-panel {
    /* 名前・色・完了・使用の4列がちょうど収まる幅 */
    width: 36;
    height: 100%;
    border: round ansi_bright_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

#tag-panel {
    width: 1fr;
    height: 100%;
    margin-left: 1;
    border: round ansi_bright_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

#status-panel:focus-within, #tag-panel:focus-within {
    border: round ansi_blue;
}

#status-table, #tag-table {
    height: 1fr;
    background: ansi_black;
}

#settings-hint, #settings-note {
    height: auto;
    margin-top: 1;
    color: ansi_bright_black;
}

#settings-buttons {
    height: auto;
    margin-top: 1;
    align-horizontal: right;
}

#settings-buttons Button {
    margin-left: 2;
}

/* ステータス・タグ1件の編集 */
#item-dialog {
    width: 52;
    max-width: 90%;
    height: auto;
    padding: 1 2;
    border: round ansi_blue;
    background: ansi_black;
    border-title-color: ansi_white;
    border-title-style: bold;
}

#item-dialog Label {
    margin-top: 1;
    color: ansi_bright_black;
}

#item-buttons {
    height: auto;
    margin-top: 1;
    align-horizontal: right;
}

#item-buttons Button {
    margin-left: 2;
}
