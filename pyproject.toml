[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "taskterm"
version = "0.1.0"
description = "ターミナルで動くTODO・タスク管理アプリ"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["todo", "task", "tui", "terminal", "calendar"]
classifiers = [
    "Environment :: Console",
    "Natural Language :: Japanese",
    "Programming Language :: Python :: 3",
]
dependencies = [
    # 端末の配色に追従する ansi-dark テーマが入ったのが 8.2.5 (それ以前は起動できない)
    "textual>=8.2.5",
    "rich>=13.0.0",
]

[project.urls]
Homepage = "https://github.com/yamato3010/taskterm"
Repository = "https://github.com/yamato3010/taskterm"
Changelog = "https://github.com/yamato3010/taskterm/blob/main/CHANGELOG.md"

[project.scripts]
taskterm = "taskterm.__main__:main"
